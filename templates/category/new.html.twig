{% extends 'dashboard/layout.html.twig' %}

{% block title %}Nouvelle catégorie — Dashboard{% endblock %}
{% block dash_title %}Nouvelle catégorie{% endblock %}
{% block dash_subtitle %}Créez une catégorie pour organiser le catalogue.{% endblock %}

{% block dash_actions %}
    <a href="{{ path('app_category') }}" class="umu-btn umu-btn--ghost umu-btn--sm">Retour à la liste</a>
{% endblock %}

{% block dash_body %}
<div class="dash-card dash-card--narrow">
    {{ form_start(form, { attr: { class: 'dash-form', 'data-turbo': 'false' } }) }}
        {{ form_row(form.name, { attr: { class: 'form-control' }, label: 'Nom de la catégorie' }) }}
        <div class="dash-form__actions">
            <button type="submit" class="umu-btn umu-btn--primary">Enregistrer</button>
        </div>
    {{ form_end(form) }}
</div>
{% endblock %}
